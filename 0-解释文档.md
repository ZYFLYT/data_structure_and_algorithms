# 数据结构与算法基础 · 核心目录文档

## 1. 内存、指针、引用与动态分配

### 1.1 基本定义
- **数据结构**：计算机组织与存储数据的方式（逻辑结构 + 物理结构）。
- **算法**：对特定数据结构执行操作的有效步骤（增、删、查、改、排序等）。
- **关系**：数据结构承载数据，算法驱动数据变化，二者相辅相成。

### 1.2 复杂度分析（大 O 表示法）
- **时间复杂度**：算法执行时间随数据规模 \(n\) 的增长率（如 \(O(1)\)、\(O(n)\)、\(O(\log n)\)）。
- **空间复杂度**：算法运行时额外消耗内存随 \(n\) 的增长率。
- **分析原则**：关注最坏情况，忽略低阶项与常数系数。

### 1.3 C++ 内存模型
- **栈内存**：函数局部变量自动分配与释放，容量小，速度快。
- **堆内存**：通过 `new` / `delete`（或 `malloc` / `free`）手动管理，容量大，生存期由程序员控制。
- **引用**：变量的别名，必须初始化，不占用额外内存（底层为指针实现）。
- **指针**：存储内存地址，支持算术运算，可重新指向，是构建链式结构的基石。

---

## 2. 链式存储 —— 单向链表

### 2.1 为什么需要链表？（对比顺序表）
| 特性 | 顺序表 (ArrayList) | 链表 (LinkedList) |
| :--- | :--- | :--- |
| **随机访问** | \(O(1)\) | \(O(n)\) |
| **头插/删除** | \(O(n)\) 数据搬移 | \(O(1)\) 仅改指针 |
| **扩容** | 需申请新内存+拷贝，代价高 | 无需连续内存，按需申请节点 |
| **空间利用率** | 可能预分配过多或扩容频繁 | 略有指针开销，但无碎片浪费 |

### 2.2 节点结构与内存拓扑
- **节点定义**：
  ```cpp
  template<typename T>
  struct Node {
      T data;       // 数据域
      Node* next;   // 指针域，指向后继节点
  };
  ```
- **拓扑特征**：每个节点在堆中独立分配，地址不连续；通过 `next` 指针串联成链，尾节点的 `next` 指向 `nullptr`。

### 2.3 核心操作注意点
- **遍历**：从 `head` 开始，沿 `next` 移动，直到 `nullptr`。
- **插入**：修改前驱节点的 `next` 指向新节点，新节点 `next` 指向原后继。
- **删除**：保存待删节点，修改前驱 `next` 绕过它，最后 `delete` 释放内存。
- **边界处理**：空链表（`head == nullptr`）和单节点操作需格外小心。

---

## 3. 双向循环哨兵链表（终极优化方案）

### 3.1 单向链表的痛点
- 无法高效获取前驱节点，删除任意节点（非头）需从头遍历至前驱，时间复杂度 \(O(n)\)。
- 尾插需遍历到末尾（除非额外维护 `tail` 指针，但删除尾节点时 `tail` 更新仍困难）。
- 大量 `if (nullptr)` 判空逻辑使代码臃肿且易错。

### 3.2 双向循环 + 哨兵设计
- **节点结构升级**：引入 `prev` 指针指向前驱。
- **哨兵（Sentinel / Dummy Node）**：
  - 一个不存储实际数据的永久节点，作为链表头部和尾部的“锚点”。
  - **空链表状态**：哨兵的 `next` 和 `prev` 均指向自己（`head->next = head; head->prev = head;`）。
  - **非空链表**：哨兵 `next` 指向首节点，`prev` 指向尾节点；首节点 `prev` 指向哨兵，尾节点 `next` 指向哨兵。

### 3.3 核心收益与代码对称性
- **零空指针**：因为哨兵永远存在，任何节点的 `prev` 和 `next` 都有效，`if` 判断几乎完全消除。
- **完全对称**：
  - 头插：在 `sentinel` 与 `sentinel->next` 之间插入。
  - 尾插：在 `sentinel->prev` 与 `sentinel` 之间插入。
  - 逻辑代码完全一致，只需传入两个相邻节点指针。
- **迭代器适配**：`begin()` 定位至 `sentinel->next`，`end()` 直接返回指向 `sentinel` 的迭代器，完美契合 STL 的 `[begin, end)` 左闭右开区间。

### 3.4 关键指针变化（以插入为例）
> 在节点 `A` 和 `B` 之间插入新节点 `X`：
> 1. `X->prev = A;` `X->next = B;`
> 2. `A->next = X;` `B->prev = X;`
> （哨兵机制保证了 `A` 和 `B` 永远存在，无需判断 `nullptr`）
