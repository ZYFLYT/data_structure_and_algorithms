我们在这里写目录文档

## 1-内存、指针、引用、动态分配
1. 数据结构与算法定义
数据结构：计算机中存储、组织数据的方式。比如数组、链表、栈、队列、树、图等。
算法：对数据进行操作的一系列步骤，比如排序、查找、遍历。
关系：数据结构是算法的基础，算法依赖数据结构来高效运作。

2. 复杂度分析（大O表示法）
我们需要学会衡量算法好坏的两个尺度：
时间复杂度：运行时间随数据规模 n n 的增长趋势。
空间复杂度：占用的额外内存随 n n 的增长趋势。

3. C++ 热身：内存、指针、引用、动态分配
数据结构的核心是内存管理。C++ 给我们直接操作内存的能力。
栈内存：局部变量自动分配释放。
堆内存：用 new / delete 手动分配释放，生存期由我们控制（数据结构底层常用）。

## 2-链式存储 —— 单向链表
  #### 内存拓扑图、指针操作的微观过程、边界条件
为什么要有链表？（对比 ArrayList 的致命伤）
我们昨天实现的 ArrayList（顺序表）虽然访问快（O(1)），但它有两个无法忍受的痛点：
插入/删除太慢：在头部插入一个元素，整个数组都要后移，时间复杂度 O(n)。如果你有 100 万个元素，插个队就要搬移 100 万次。
扩容代价高昂：扩容时不仅要申请新内存，还要复制所有旧数据，且原有迭代器全部失效。
链表（Linked List） 的出现就是为了解决这两个问题。它的核心理念是：“不连续存储，用指针串联”。

链表的内存拓扑（极其重要）
1. 节点（Node）的构成
链表由一个个 节点（Node） 组成。每个节点在堆内存中独立存在，地址不连续。每个节点包含两部分：
数据域：存储实际的数据（T data）。
指针域：存储下一个节点的内存地址（Node* next）。